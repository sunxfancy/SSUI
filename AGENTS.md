# Agent 指引

AI 助手在本仓库工作时，请先阅读根目录 **[CLAUDE.md](./CLAUDE.md)**，其中包含：

- Monorepo 结构与各子包职责
- 验证 / 编译命令速查表
- 根目录与子项目 `package.json` 脚本说明
- 常用开发场景与服务端口

构建、测试、打包前务必查阅对应目录的 `package.json`，使用项目已定义的 `yarn` 命令。
